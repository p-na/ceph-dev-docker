#!/bin/bash

if (( "$#" > 0 )) ; then
    if [[ "${1}" =~ .*noc.* ]] ; then
        args="--no-cache"
    else
        echo "Unrecognized argument: ${1}"
        exit 1
    fi
fi

cd ~/src/ceph-dev-docker
docker build -t ceph-dev:centos7 ${args} .
cd -
